pipeline {
    agent {label 'master'}
    stages {
        stage ('Build Now'){
            input {
                
            }
            steps {
                echo "This was built just now."
            }
        }
    }
}