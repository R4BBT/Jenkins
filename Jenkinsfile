pipeline {
    agent {label 'master'}
    stages {
        stage ('Build') {
            echo 'building...'
        }

        stage ('Test') {
            echo 'Testing'
        }

        stage ('Deploy') {
            echo 'Deploying'
        }
    }
}